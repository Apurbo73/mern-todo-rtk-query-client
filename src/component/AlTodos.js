import React from 'react';

const AlTodos = ({t}) => {
    return (
        <div>
     <div className="btn btn-dark mb-2 mt-2">{t.data}</div>            
        </div>
    );
};

export default AlTodos;